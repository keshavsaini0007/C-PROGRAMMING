# include <stdio.h>
# include <string.h>

int main (){
    char str[999];
    puts("enter your text : ");
    gets(str);

    

    puts("the reversed text was  : ");

    
    puts(str);
    return 0;
}